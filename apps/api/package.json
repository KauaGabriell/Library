{
  "name": "@library/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx --env-file=.env --watch src/server.ts",
    "typecheck": "tsc --project tsconfig.json",
    "test": "vitest",
    "test:watch": "vitest watch"
  },
  "dependencies": {
    "fastify": "^5.12.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.23.12",
    "vitest": "^4.1.11"
  }
}
